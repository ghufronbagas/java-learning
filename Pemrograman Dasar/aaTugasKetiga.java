import java.util.*;

public class aaTugasKetiga {
    public static void main(String[] args){
    Scanner input = new Scanner(System.in);

    int a,b,z,n;

    n = input.nextInt();
    a = input.nextInt();
    b = input.nextInt();
    z = 0;
    
    

        if (n > 0){
            if (a > b){
                z = a;
            }else {z = b;
            }
        }
        System.out.println(z);

        /* else diatas adalah milik if yang ada didalam if yang pertama, hal ini ditunjukan dari perintah yang ada didalam else tersebut lebih berpasangan
        dengan perintah yang ada didalam if yang kedua(didalam if pertama)
        */








    }
    
}
